import React, { Component } from 'react';

class Film extends Component {
    //state = {  }
    render() {        
        return ( 
            <>
                {/* <ul>
                    {this.props.films.map(e => (
                        <li key={e}>{e}</li>
                    ))}
                </ul> */}
                <p>dudupauap</p>
                {/* //<p>Film component {this.props.films}</p> */}
            </>
         );
    }
}
 
export default Film;